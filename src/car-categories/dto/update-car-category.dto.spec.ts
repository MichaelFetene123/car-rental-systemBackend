import { UpdateCarCategoryDto } from './update-car-category.dto';

describe('UpdateCarCategoryDto', () => {
  it('should be defined', () => {
    expect(new UpdateCarCategoryDto()).toBeDefined();
  });
});
