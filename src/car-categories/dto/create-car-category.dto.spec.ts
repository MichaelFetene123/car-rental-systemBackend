import { CreateCarCategoryDto } from './create-car-category.dto';

describe('CreateCarCategoryDto', () => {
  it('should be defined', () => {
    expect(new CreateCarCategoryDto()).toBeDefined();
  });
});
